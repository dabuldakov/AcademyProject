spring:
  jpa:
    show-sql: true
    hibernate:
      #      ddl-auto: validate
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: ChangePassword
    url: jdbc:postgresql://localhost:5440/publisher